---
name: Test Action

on:
  push:

jobs:
  test:
    name: Test Action
    runs-on: ubuntu-20.04
    strategy:
      fail-fast: false
      matrix:
        float: [32, 64]
        msystem: [ucrt, mingw64]
    steps:
      - name: test
        run: |
          echo test
